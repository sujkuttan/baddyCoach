[pytest]
markers =
    gpu: tests requiring CUDA GPU
    cpu_only: tests that should only run on CPU
    model: tests requiring ML model checkpoints on disk
    memory_intensive: tests using >2GB RAM
    slow: tests taking >10 seconds
    integration: full pipeline integration tests
